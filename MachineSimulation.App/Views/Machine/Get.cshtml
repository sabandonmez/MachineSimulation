<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leadyang CNC</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .row {
            align-items: start;
        }

        #uretimSureleriListesi {
            padding-left: 0;
        }

            #uretimSureleriListesi li {
                list-style: none;
            }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <h1>@Model.MachineName</h1>
                <input class="form-control mb-2" placeholder="Sıcaklık">
                <input class="form-control mb-2" placeholder="Hız">
            </div>
            <div class="col-md-6">
                <h2>İşlem Süreleri</h2>
                <button id="hazirlikBaslat" class="btn btn-danger mb-2">Hazırlık Başlat</button>
                <div>Süre: <span id="hazirlikSuresi">0</span>s</div>
                <button id="uretimBaslatDurdur" class="btn btn-danger mb-2">Üretim</button>
                <div>Süre: <span id="uretimSuresi">0</span>s</div>
                <ul id="uretimSureleriListesi"></ul>
                <button id="uretimiDurdur" class="btn btn-danger mb-2">Üretimi Durdur</button>
                <div id="toplamSuresi"></div>
                <div>Adet: <span id="uretimAdeti">0</span></div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.7.12/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', (event) => {
            updateNavbarTitle('@Html.Raw(Model.MachineName)');
        });

        document.addEventListener('DOMContentLoaded', (event) => {
            updateNavbarTitle('@Html.Raw(Model.MachineName)');
            showBackIconAndRedirect(); // Navbar'da geri dönme ikonunu aktif et
        });

        function updateNavbarTitle(machineName) {
            var navbarTitle = document.getElementById('navbar-title');
            if (navbarTitle) {
                navbarTitle.textContent = machineName;
            }
        }
     
    </script>
</body>
</html>
